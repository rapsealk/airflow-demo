services:
  airflow:
    image: apache/airflow:3.0.2rc2-python3.12
    ports:
      - 8080:8080
    environment:
      AIRFLOW_HOME: /opt/airflow
    volumes:
      - .:/opt/airflow
      - ./dags/torchtune/v1/.env:/opt/airflow/dags/torchtune/v1/.env:ro
    command: airflow standalone
